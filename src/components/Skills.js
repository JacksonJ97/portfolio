// Components
import SectionHeader from "./Utilities/SectionHeader";

const Skills = ({ id }) => {
  return (
    <div id={id}>
      <SectionHeader text="Skills" />
    </div>
  );
};

export default Skills;
